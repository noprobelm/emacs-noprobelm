;;; np-term.el --- Modules -*- lexical-binding: t; -*-
;;; Commentary:
;;; Code:
(use-package vterm
  :straight t
  :config
  (setq vterm-always-compile-module t)
  )

(provide 'np-term)
;;; np-term.el ends here.
